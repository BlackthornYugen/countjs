doctype html
html
  head
    title= title
    if subdomain
      link(rel='stylesheet', href=`/stylesheets/${subdomain}.css`)
    else
      link(rel='stylesheet', href='/stylesheets/style.css')
  body
    block content
